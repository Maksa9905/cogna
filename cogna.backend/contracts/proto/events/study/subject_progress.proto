syntax = "proto3";

package events.study.v1;

// Topic: study.delete.subject.progress
// Producer: content-service -> Consumer: study-service
message DeleteSubjectProgressEvent {
  string subject_id = 1;
  string user_id = 2;
}
