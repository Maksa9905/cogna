syntax = "proto3";

import "events/assessment/assessment.proto";

package assessment.v1;

service AssessmentService {
  rpc ProcessTranscription(events.assessment.v1.AssessmentProcessEvent)
      returns (ProcessResponse);
}

message ProcessResponse {
  bool success = 1;
}
